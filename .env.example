# Variáveis de ambiente para produção
# Copie para .env e preencha

# Não é mais necessário definir VITE_API_URL — o docker-compose.prod.yml
# usa /api (relativo) por padrão, o que funciona com qualquer IP ou domínio.
# Edite apenas o backend/.env com JWT_SECRET, FRONTEND_ORIGIN e NODE_ENV.
